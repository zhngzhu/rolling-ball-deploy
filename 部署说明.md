# 滚动小球游戏服务器 · 部署说明

这是一个**纯 Python 写的网页小游戏服务器**，自带游戏前端页面。把整个文件夹放到一台有公网 IP 的服务器上，装好 Python，跑一条命令，玩家就能用浏览器进来玩。

无需安装任何第三方库、无需数据库、无需 Docker（当然用 Docker 也行，见末尾）。

---

## 一、文件夹里有什么

| 文件/目录 | 作用 |
|---|---|
| `server.py` | 后端主程序（游戏逻辑 + 网页接口 + 排行榜/抽卡/竞速/后台） |
| `race_server.py` | 竞速模式模块（被 server.py 调用，必须一起带） |
| `upstash_store.py` | 存储助手（默认不用，留着即可，别删） |
| `rolling-ball/` | 游戏前端网页（index.html、admin.html、图片等） |
| `game_data.json` | **玩家存档**，游戏进度都存在这里（首次启动会自动从 `seed_data.json` 生成，含 24 名玩家真实数据；该文件不入库） |
| `seed_data.json` | 初始存档（24 名玩家真实数据），首次启动自动播种成 `game_data.json`，可留着 |
| `.admin_password` | 后台密码文件（首次启动自动生成，默认 `pc767d54`） |
| `start.sh` | 一键启动脚本（后台运行） |
| `stop.sh` | 一键停止脚本 |
| `rolling-ball.service` | systemd 守护配置（开机自启 + 崩溃自拉，推荐用） |
| `requirements.txt` | 依赖说明（**什么都不用装**） |

> ⚠️ `game_data.json` 就是玩家的全部家当。换服务器、备份、迁移，只要拷这个文件就行。

---

## 二、第一步：服务器准备（装 Python）

登录服务器（SSH 或厂商网页终端），确认 Python 版本 ≥ 3.10：

```bash
python3 --version
```

- 有 3.10+：跳过。
- 没有 / 版本太低（例如只有 3.8、3.9）：
  - **Ubuntu/Debian**：`sudo apt update && sudo apt install -y python3`
  - **CentOS/Rocky/Alma**：`sudo dnf install -y python3`
  - 装完再 `python3 --version` 确认。

> ⚠️ **本服务器最高支持到 Python 3.10**（跑不了 3.11+）。请确认装的是 **3.10.x**（3.9 也行，但建议 3.10）。
> 这个游戏**只用到 Python 标准库，零第三方依赖**，代码里没有任何 3.11 才有的语法，在 3.10 上能直接跑，不用 `pip install` 任何东西。

---

## 三、第二步：把代码弄到服务器上

### 方式 1：从 GitHub 拉取（推荐，更新最方便）

在服务器上直接克隆仓库（需要 Git，一般系统自带；没有就 `sudo apt install -y git`）：

```bash
cd /opt
sudo git clone https://github.com/zhngzhu/rolling-ball-deploy.git rolling-ball
cd /opt/rolling-ball
ls        # 应看到 server.py、rolling-ball/、seed_data.json、start.sh 等
```

以后要更新，进到目录 `git pull` 再重启即可：
```bash
cd /opt/rolling-ball
git pull
bash stop.sh; bash start.sh
```

> 仓库地址以实际为准；如果设为私有仓库，克隆时需输入有读取权限的 GitHub 账号。

### 方式 2：下载 ZIP / scp 传文件夹（不装 Git 也行）

- **下载 ZIP**：在 GitHub 仓库页面点 `Code → Download ZIP`，传到服务器解压到 `/opt/rolling-ball`。
- **scp 直传**（自己电脑终端）：`scp -r Servers 用户名@服务器IP:/opt/rolling-ball`

进到目录确认：
```bash
cd /opt/rolling-ball
ls        # 应看到 server.py、rolling-ball/、seed_data.json、start.sh 等
```

> 注意：仓库里**不含** `game_data.json`（那是运行时玩家存档）。首次启动会自动从 `seed_data.json` 播种出 24 名玩家的初始存档，无需手动准备。

---

## 四、第三步：开放端口（最关键，不然外网进不来）

**光在服务器上跑起来还不够**，云厂商默认防火墙会把外部访问挡住。必须去**云厂商控制台**放行 TCP 端口 **8080**（入站）。

- **阿里云 / 腾讯云 / 华为云**：控制台 → 该服务器 → **安全组** → 入站规则 → 允许 `TCP 8080` 来源 `0.0.0.0/0`。
- 如果服务器本身也开了 `firewalld`/`ufw`，同样要放行：
  ```bash
  sudo ufw allow 8080/tcp          # Ubuntu ufw
  # 或
  sudo firewall-cmd --add-port=8080/tcp --permanent && sudo firewall-cmd --reload   # CentOS
  ```

> 不开放端口，外面永远打不开游戏页面。这一步忘了是最常见的"为什么连不上"。

---

## 五、第四步：启动游戏

### 方式 A：start.sh 后台跑（最简单）

```bash
cd /opt/rolling-ball
bash start.sh
```

启动后看日志：`tail -f server.log`。看到 `Game server running on port 8080` 就成功了。
停止：`bash stop.sh`。

### 方式 B：systemd 守护（推荐，开机自启 + 崩了自动重启）

```bash
# 1) 把 rolling-ball.service 放到系统目录（先按实际情况改里面的 User 和路径）
sudo cp rolling-ball.service /etc/systemd/system/rolling-ball.service

# 2) 重载并启动
sudo systemctl daemon-reload
sudo systemctl enable --now rolling-ball

# 查看状态 / 日志
sudo systemctl status rolling-ball
sudo journalctl -u rolling-ball -f
```

以后服务器重启会自动起来，不用手动管。

---

## 六、第五步：验证能玩

浏览器打开：`http://<服务器公网IP>:8080/`

能看到游戏页面、能注册/登录/玩，就成功了。

> 想用 `https` 和域名（看起来更正规、不会被浏览器标"不安全"），见末尾"反向代理"一节。

---

## 七、管理后台怎么进

浏览器打开：`http://<服务器公网IP>:8080/admin.html`

- 账号：`user`
- 密码：`pc767d54`

后台里能看玩家数、排行榜、封禁账号/IP、发公告等。

> 密码在 `start.sh` 的 `ADMIN_PASS` 和 `.admin_password` 文件里，想改就改这两处（两处保持一致）。

---

## 八、数据备份与迁移

所有玩家数据都在 `game_data.json`（在游戏目录里，或你设的 `DATA_DIR` 卷里）。

- **备份**：直接拷这个文件即可，例如定时任务：
  ```bash
  cp /opt/rolling-ball/game_data.json /opt/rolling-ball/backup/$(date +%F).json
  ```
- **迁移到新服务器**：把整个文件夹（尤其 `game_data.json`）拷过去，按上面步骤重跑就行。

---

## 九、（可选）用域名 + HTTPS：nginx 反向代理

如果想让玩家用 `https://你的域名` 访问（不要 `:8080` 端口、更正规），在服务器上装 nginx：

```bash
sudo apt install -y nginx        # Debian/Ubuntu
```

`/etc/nginx/sites-available/rolling-ball` 写：
```nginx
server {
    listen 80;
    server_name 你的域名;          # 例如 game.example.com
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```
启用：`sudo ln -s /etc/nginx/sites-available/rolling-ball /etc/nginx/sites-enabled/ && sudo nginx -t && sudo systemctl reload nginx`

然后**去域名 DNS 把域名 A 记录指向服务器公网 IP**，并在云安全组放行 80（和 443 若配 HTTPS）。
`start.sh` 里 `TRUST_XFF=1` 已经开了，nginx 转发的真实玩家 IP 能正确拿到。

> HTTPS 证书（Let's Encrypt）可选，自行搜 `certbot nginx` 教程，不配也能玩，只是浏览器地址栏会提示"不安全"。

---

## 十、常见问题排查

| 现象 | 原因 / 解决办法 |
|---|---|
| 浏览器打不开 / 超时 | ① 服务器进程没起（看 `server.log`）；② **安全组没放行 8080**（最常见）；③ 防火墙挡了 |
| 看到页面但点登录没反应 | 看 `server.log` 报错；确认 `race_server.py`、`upstash_store.py` 和 `server.py` 在同一目录 |
| 后台 admin.html 进不去 / 401 | 账号 `user`，密码 `pc767d54`；若改过密码以 `.admin_password` 文件为准 |
| 玩家 IP 全显示 127.0.0.1 | 用了 nginx 反向代理但没开 `TRUST_XFF=1`，在 `start.sh` / service 里设 `TRUST_XFF=1` |
| 服务器重启后游戏没了 | 没设 `DATA_DIR` 且目录在临时盘；用 systemd 方式启动，数据就在游戏目录里持久保存 |

---

## 十一、安全提醒

- 公网暴露后可能被扫描/攻击。后台有密码 + 失败锁定保护，已较稳。
- 若担心，可把 `start.sh` 里 `ADMIN_REMOTE` 改成 `0`，这样后台只能 SSH 连上服务器后从本机访问（或 SSH 端口转发），外网进不去。
- 定期备份 `game_data.json`。
- 不建议用 root 跑游戏进程；用普通用户（systemd 里改 `User=`）。

---

## 附：用 Docker 也行（如果你习惯容器）

服务器装好 Docker 后，在 `/opt/rolling-ball` 建 `Dockerfile`：
```dockerfile
FROM python:3.10-slim
WORKDIR /app
COPY . /app
EXPOSE 8080
CMD ["python3", "server.py"]
```
构建运行：
```bash
docker build -t rolling-ball .
docker run -d --name rolling-ball -p 8080:8080 -v rb-data:/app rolling-ball
```
数据落在 `rb-data` 卷里，删容器不丢档。
